public class ExtraCode {
    double random;
    /*
    {
        for (int x = 0; x < width; x++) {
            for (int y = 0; y < height; y++) {
                random = Math.random() * 3d;
                if (random > 2) {
                    pixels[x][y] = blue;
                } else if (random > 1) {
                    pixels[x][y] = green;
                } else {
                    pixels[x][y] = red;
                }
            }
        }
    }

    {
        for (int y = 0; y < height; y++) {
            random = Math.random() * 3;
            for (int x = 0; x < width; x++)
                if (random > 2) {
                    pixels[x][y] = green;
                } else if (random > 1) {
                    pixels[x][y] = blue;
                } else {
                    pixels[x][y] = red;
                }
        }
    }

     */
}
